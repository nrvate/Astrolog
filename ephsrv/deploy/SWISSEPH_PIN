# The thread-safe Swiss Ephemeris fork astrolog-ephd is built against, by
# commit: tools/ephsrv-fork.sh and the container image fetch exactly this,
# so a server binary is reproducible from one commit of this repository.
# nrvate/swisseph, tag v2.10.03-ts.14 (2026-09-17). Move it deliberately,
# after the eight server gates pass against the new commit.
SWE_REPO=https://github.com/nrvate/swisseph.git
SWE_COMMIT=47671e5f9c036181d9d5fab8049ae0f5fc151dd0
